{
  "name": "ttech-site",
  "version": "1.0.1",
  "private": true,
  "description": "Ttech professional website (Render-ready, rebrandable)",
  "scripts": {
    "start": "node server.js",
    "brand:preview": "node scripts/brand-swap.js --config brand.ttech.json --dry-run",
    "brand:apply": "node scripts/brand-swap.js --config brand.ttech.json"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.21.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0",
    "nodemailer": "^6.9.14"
  }
}
